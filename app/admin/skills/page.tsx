export default function SkillsAdminPage() {
    return (
        <div className="max-w-4xl mx-auto">
            <h1 className="text-3xl font-bold gradient-text mb-4">Skills Manager</h1>
            <p className="text-gray-400">
                Follow the same pattern as the Projects page to create this CRUD interface.
                Include: name, category, proficiency (1-100), icon, and order_index.
            </p>
        </div>
    );
}
