export default function AboutAdminPage() {
    return (
        <div className="max-w-4xl mx-auto">
            <h1 className="text-3xl font-bold gradient-text mb-4">About Section Editor</h1>
            <p className="text-gray-400">
                Follow the same pattern as the Hero editor to create this page.
                Include title, content (textarea), image upload, and resume URL fields.
            </p>
        </div>
    );
}
